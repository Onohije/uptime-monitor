# Local state files — never commit these
*.tfstate
*.tfstate.*
.terraform/
.terraform.tfstate.lock.info

# Variable files that may hold secrets
*.tfvars
*.tfvars.json

# Crash logs
crash.log
crash.*.log
tfplan
